# name: test/fuzzer/public/pivot_empty_enum.test
# description: Test pivot with an empty enum
# group: [public]

statement ok
CREATE TABLE t1 (c01 INT);

# a bare ENUM names the empty enum type, so the pivot produces no columns at all
statement error
PIVOT_WIDER t1 ON c01 IN ENUM;
----
SELECT list is empty after resolving * expressions